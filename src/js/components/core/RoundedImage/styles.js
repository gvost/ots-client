
import { StyleSheet } from 'react-native'
import CORE from '@styles'

export default StyleSheet.create({
  container: {
    flex: 1
  },
  image: {
    width: 60,
    height: 60,
    borderRadius: 30
  }
})

